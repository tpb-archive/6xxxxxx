Install Instructions:

after it downloads open the folder and extract the file, it should be a setup file, then install the file, i put to installs in 1 file so you didnt have to download ragray first, so make sure everything that installs goes into the same folder.